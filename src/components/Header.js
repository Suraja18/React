import React from 'react'

// function Header() {
//   return (
//     <h1>List of Products</h1>
//   )
// }

const Header = () => (
    <div>

        <h1>List of Products</h1>
    </div>
)  // we can use export at start so that in app.js we must use Header not MyHeader eg, export const

export default Header